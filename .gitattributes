# Auto-detect text files and normalize line endings
* text=auto

# Shell scripts must have LF line endings and be executable
*.sh text eol=lf
scripts/*.sh text eol=lf

# PowerShell scripts keep CRLF on Windows
*.ps1 text eol=crlf

# Mark all shell scripts as executable after checkout
*.sh linguist-language=Shell
